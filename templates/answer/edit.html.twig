{% extends 'base.html.twig' %}

{% block title %}{{'Edit Answer'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Edit Answer'|trans}}</h1>

    {{ include('answer/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('answer_index') }}"><i class="fas fa-reply"></i></a>

    {{ include('answer/_delete_form.html.twig') }}
{% endblock %}
